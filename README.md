# Day 6 topics covered:

* We discussed all the advantages of using functions
* We also discussed variable scopes inside and outside of functions
* We discussed briefly how ES6 handles classes 
* We also discussed how JS implements Object/classes using function
* We did some algorithms
